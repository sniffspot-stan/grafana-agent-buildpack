#!/bin/sh

BUILD_DIR=$1

cat << EOF
---
default_process_types:
  web: AGENT_MODE=flow bin/grafana-agent run grafana-agent.env.cfg 
  worker: AGENT_MODE=flow bin/grafana-agent run grafana-agent.env.cfg 
EOF
